# Dependencies
/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
/dist_electron
/dist

# Electron-builder cache / temp files (might be system-wide, but good to have locally too)
.electron-builder/
*.dmg
*.exe
*.msi
*.AppImage
*.deb
*.rpm
*.snap
*.nsis.zip
*.blockmap

# Operating System files
.DS_Store
Thumbs.db
Desktop.ini

# IDE / Editor specific files
.vscode/
.idea/
*.suo
*.user
*.sln.docstates

# Log files (if you have custom ones you don't want to commit)
*.log
logs/
!logs/.gitkeep # Example if you want to keep the logs folder structure but not files

# User-specific configuration (if any) - example
# config.json

# Temporary files
*.tmp
*.bak
*.swp

# Electron-store data file (usually in user data directory, but good to list if one ends up in project)
# Consider if you *want* default settings committed, but actual user data should never be.
# If electron-store creates a file in your project (it shouldn't by default for user data), add its name.